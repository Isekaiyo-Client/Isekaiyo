[package]
name = "ikk-core"
description = "Isekaiyo core: error taxonomy, identifiers, task events, config primitives"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
# Name-based (v3) UUIDs for offline profiles — same RustCrypto family the
# engine already uses; MIT OR Apache-2.0.
md-5.workspace = true

[lints]
workspace = true
